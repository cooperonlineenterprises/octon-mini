# Long-Running Work Program Validation

## Evidence status

- Observation date: 2026-08-20 America/Chicago.
- Source revision: `63c6bd48ce3744fc25846bacc4860215d177428b` plus the disclosed uncommitted 4.1.0 program and final-review corrections.
- Operator class: familiar implementing agent; no human participant was simulated.
- External effects: none. The ambiguous-effect exercise used the existing local bare-Git fixture.
- Overall finding: strong source, synthetic, and disposable-project evidence;
  final-candidate real-project maturity remains unestablished.

These observations preceded the clean local candidate. Under
`docs/REAL_PROJECT_VALIDATION.md`, they can reveal and resolve defects but
cannot become final-candidate field evidence. A clean immutable revision must
rerun the matrix; its evidence belongs outside that already committed source
tree.

## Completed disposable exercises

| Scenario | Evidence | Result and limitation |
|---|---|---|
| Greenfield software and local mutation | `test_long_running_work.py` actual transaction/validation/checkpoint/task-close case | Passed; synthetic project and familiar implementing agent |
| Non-software research/context pressure | `test_long_running_work.py` source-linked comparison inputs, conflict text, irrelevant files, fixed budget | Passed; deterministic manifest, visible omissions, no content persistence or network |
| Dirty established-project adoption | `test_velocity_workflows.py` dirty repository adoption case | Passed; constructed temporary repository, not an independent real project |
| Legacy and 4.0 migration | 3.1 cross-brand suite and `test_migration_4_0_0_to_4_1_0.py` | Passed; deterministic synthetic predecessor and released-tag snapshot |
| Unattended continuation | functional limit, context, progress, pause/restart, completion cases | Passed; external worker behavior is represented by deterministic test inputs, not a hosted model |
| Instruction/adoption change | changed instruction and superseded adoption cases | Passed; continuation stopped before another iteration |
| Repeated validation failure | repeated exact failure-signature case | Passed; stopped by unchanged configured limit |
| No progress and budget exhaustion | no-progress, elapsed-time, and context-budget cases | Passed; no post-exhaustion iteration admitted |
| Checkpoint interruption | checkpoint content/marker fault suite | Passed; orphan content refused and newest committed marker recovered exactly |
| Corrupt history | malformed-tail case | Passed; original bytes preserved and reliance refused |
| Ambiguous external acknowledgement | existing interrupted local-push/read-back case | Passed; no duplicate push; not provider evidence |
| Final-review hardening | focused lifecycle, clock, counter, symlink, receipt, decision-digest, corrupt-projection, history-bound, unregistered-path, and interruption fixtures | Passed after one retained diagnostic-projection test failure and correction; still synthetic |

## Validation and performance evidence

- The pre-review full source validator passed the program tree, covering 201
  required paths, 103 default templates, all profiles and both layouts. Exact
  clean-candidate revalidation remained pending at this source-record boundary.
- Acceptance: passed with criteria 1–20 reconciled; criterion 19 is automated
  and criterion 20 remains project-demonstration-required.
- Long-running functional suite after final review: 18 cases.
- Checkpoint/history fault suite after final review: 9 cases.
- Package lifecycle suite after final review: 8 cases, including all profiles.
- Work completion: 11 cases; velocity: 5; guided setup: 28; launchers: 7.
- Same-product 4.0→4.1 migration: passed with dormant package route, no
  automatic install/adoption, stale reapply refusal, and exact rollback.
- The pre-review long-running 10,000-file benchmark passed. Warm p90 was approximately
  0.120 s context, 0.120 s status, 0.483 s resume, and 0.114 s explain; output
  was byte-deterministic and every command exited zero.
- Pre-review benchmark-v2 post-optimization runs 4–6: 129/129 samples each and all
  thresholds passed. High Assurance scaffold p90 was 8.531–8.772 s; 10k check
  p90 1.392–1.429 s; 10k mutation p90 5.918–6.052 s; 20k mutation p90
  6.605–6.645 s.

## Retained failed evidence

1. The first acceptance invocation was interrupted after roughly fourteen
   minutes while the installer recursively ran the exhaustive source matrix.
   The installed-source validator now has an explicit bounded smoke scope; the
   exhaustive matrix remains mandatory in the source checkout. The acceptance
   rerun passed.
2. Pre-optimization benchmark-v2 run 3 failed the unchanged High Assurance
   scaffold p90 at 10.106 s. Two validation-proof integration tests were found
   inside the bounded fast tier. They remain fully exercised by integration and
   release tiers but no longer burden fast mutation. Three independent
   post-change benchmark-v2 runs then passed. The failure remains recorded here
   and was not reclassified, but its former temporary JSON report was no longer
   available at the 2026-08-20 clean-candidate baseline; new candidate reports
   must therefore be retained separately.
3. A final-review package diagnostic test initially failed because the core
   checker collapsed the package's precise finding into a generic failure. The
   diagnostic projection was corrected and the complete package suite rerun;
   the original failure remains part of the task evidence.

## Required `not_run` findings

| Scenario | Status and limitation |
|---|---|
| Unfamiliar operator | `not_run`; no authorized participant without prior Octon Mini knowledge was available |
| Genuine pair/tiny-team review | `not_run`; no eligible independent human reviewer was available; agent/self-review was not substituted |
| Independent real-world dirty repository | `not_run`; authorization covered only constructed disposable subjects |
| Actual historical 3.1 project | `not_run`; the predecessor was a deterministic synthetic preimage |
| Real provider or credentialed effect | `not_run`; credentials and provider mutations were unauthorized |
| Human comprehension/confusion timing | unavailable; machine times are not human usability evidence |
| Other operating systems | `not_run` locally; the source matrix was not externally dispatched by this program |
| Final-candidate real-project evidence | `not_run`; no clean immutable implementation revision exists |

## Maturity finding

The optional single-worker stack is implemented and strongly validated at the
source and disposable-project levels. Its source architecture was accepted
under `SRC-DEC-0018` on 2026-08-20. It remains unreleased, unadopted by any real
target project, and not final-candidate mature under the existing protocol.
Implementation and passing tests create no authority, adoption, release,
project readiness, production readiness, or efficacy claim.
